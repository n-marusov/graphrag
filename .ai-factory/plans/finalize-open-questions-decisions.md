# План: Фиксация решений по открытым вопросам в спецификациях GraphRAG

**Ветка:** `main` (план без создания ветки; для реализации рекомендуется `feature/finalize-open-questions-decisions`)
**Дата:** 2026-08-30
**Источник:** ответы стейкхолдера на `specs/open-questions.md` (2026-08-30) + критический разбор ответов

## Settings

- **Testing:** yes (гейты согласованности: G1-LINK, G1-ID, G1-TRACE, G2-CONSIST, G2-QMATRIX, G2-SEC; QA-метрики закрывают TBD)
- **Logging:** minimal (документная работа, кода нет)
- **Docs:** yes (план сам является документацией; изменения — только в `specs/`, `.ai-factory/`)

## Research Context

- RESEARCH.md Active Summary устарел (описывал создание видения, которое уже выполнено).
- Актуальный вход для плана — ответы стейкхолдера на открытые вопросы (2026-08-30) и критический разбор: 6 прямых противоречий с принятыми артефактами (A1–A6), 9 частичных ответов с интерпретацией по умолчанию (B1–B9), 13 полных ответов (C).
- Дефолты плана: US расширяются доменами ЖЦ; корпоративный Keycloak в периметре; Vue 3 для фронтенда; «система не оператор ПДн»; RPO = 0 для исходников; RAGAS для оценки ответов.

## Допущения (дефолты критического разбора — заменить при ином решении стейкхолдера)

1. US покрывают этапы ЖЦ: новые домены `US-ops.*` и `US-decommission.*` (ответ A4, вопрос 1.2).
2. SSO — существующий корпоративный Keycloak (self-hosted, в периметре); система подключается как клиент (A1, вопрос 1.10).
3. Фронтенд-фреймворк — Vue 3 + TypeScript (1.9; кандидат React — решение фиксируется в ADR T2).
4. «Система не оператор ПДн»: корпус — код/спеки/документы; SSO-идентификация — минимальна, по корпоративной политике; фильтрация утечек блокирует ПДн (A2, 2.1).
5. RPO: 0 для исходных артефактов (источник правды — GitLab/S3, вне системы); индекс восстанавливается полной перестройкой; бюджет простоя ≤ 15 мин/день на инстанс (B1, 1.3/1.8).
6. Инструмент оценки качества ответов — RAGAS (eval-контур в CI, внешний процесс; B7, 3.5).
7. Внешняя LLM «по договору» — только через корпоративный шлюз с фильтрацией и аудитом (A3, 1.7).

---

## Фаза 1: Устранение противоречий артефактов (A1–A6)

### Задача 1.1 — Согласовать языковой стек: Go + TypeScript (A1-предшественник: 1.9)

- [x] Исправить `specs/adr/README.md` §«Языки реализации»: удалить строки про Rust (CPU-компоненты) и Python-прототип; зафиксировать: серверная кодовая база — Go (канон `ADR-IMPL.STACK.go-single-language-adoption`), фронтенд — TypeScript (ссылка на новый ADR T1.2); добавить пометку, что RESEARCH.md Decisions (язык) приведён к канону.
- [x] Привести `.ai-factory/RESEARCH.md` (Decisions, язык) к канону Go-only: убрать «Rust — только in-process CPU-тяжёлые…; прототип — Python».
- [x] Проверить `specs/qa/gates.md` `AG-ADR` (пример про Go — единственный принятый ADR): обновить формулировку с учётом нового ADR на TypeScript-фронтенд.
- **Deliverable:** нет остаточных упоминаний Rust/Python в кодовой базе; стек однозначен: Go (сервер) + TypeScript (SPA).
- **Зависит от:** —

### Задача 1.2 — Новый ADR: TypeScript-фронтенд (1.9)

- [x] Создать `specs/adr/ADR-DES.UI.spa-typescript-frontend.md` (статус ПРИНЯТО):
  - Контекст: веб-интерфейс — тонкий SPA в браузере (решение API Gateway ADR); фронтенд вне серверной кодовой базы Go.
  - Решение: TypeScript; фреймворк — Vue 3 (дефолт плана; альтернатива — React — зафиксировать выбор); сборка Vite; контракт с API — OpenAPI 3.1 (клиент генерируется из схемы).
  - Рассмотренные альтернативы: Go-шаблоны/go:embed (отклонено ранее в API Gateway ADR), vanilla JS, Svelte.
  - Последствия: независимый жизненный цикл фронта (уже зафиксирован в API Gateway ADR); отдельный тулчейн (Node); контрактные тесты на кэш-заголовках и SPA-fallback.
- [x] Обновить `specs/adr/README.md` (реестр: 8 принятых ADR).
- **Deliverable:** ADR `ADR-DES.UI.spa-typescript-frontend` ПРИНЯТО, реестр синхронизирован.
- **Зависит от:** —

### Задача 1.3 — Заменить BR «no-user-auth» на модель доступа SSO + read-only (1.10)

- [x] Создать `specs/business-rules/BR-constraint.sso-readonly-access.md` (тип `constraint`, High):
  - Формулировка: доступ людей — через корпоративный SSO (Keycloak); по умолчанию один пользователь `User`; ролей нет (RBAC отсутствует); все запросы — только на чтение (read-only: никаких write-операций через API/UI/MCP); ИИ-агенты — по токену (агентский токен, выдаётся через корпоративный процесс); граница: `REQ-NFR-security.compliance.llm-contour` действует (фильтрация утечек, события безопасности); развёртывание — в доверенном периметре.
  - Критерии проверки: отсутствие write-эндпоинтов в контракте API (детерминированный перечень); неавторизованная попытка → отказ с кодом ошибки; агентский запрос без токена → отказ; события фильтрации утечек фиксируются.
- [x] Обновить `specs/business-rules/BR-constraint.no-user-auth-in-mvp.md`: статус ЗАМЕНЕНО, ссылка на новый BR (правило BR-README: изменения — через статус, не удаление).
- [x] Обновить `specs/business-rules/README.md`: покрытие типов (constraint: 4 → 5; счётчик правил 5 → 6); список кандидатов — убрать устаревшие.
- **Deliverable:** BR-модель доступа согласована с ответом 1.10; BR README актуален.
- **Зависит от:** —

### Задача 1.4 — Новый ADR: корпоративный SSO (Keycloak) (1.10)

- [x] Создать `specs/adr/ADR-DES.SECURITY.sso-keycloak.md` (статус ПРИНЯТО):
  - Контекст: агенты могут обращаться и через человеческий шлюз (API Gateway) → нужна аутентификация людей; агенты — токен.
  - Решение: аутентификация людей — через корпоративный SSO (Keycloak, self-hosted в периметре, существующий корпоративный инстанс); API Gateway выполняет проверку токена (OIDC); агенты — статический/корпоративный токен, read-only; RBAC не вводится (один пользователь по умолчанию).
  - Альтернативы: внешний IdP (нарушает периметр), самописная авторизация (отклонено), отсутствие авторизации (противоречит ответу 1.10).
  - Последствия: зависимость от доступности Keycloak (внешняя система на пути аутентификации; кэш/fallback для read-only контура); обработка идентификационных данных — минимальна, по корпоративной политике (см. T1.5); MFA — на стороне Keycloak.
- [x] Обновить `specs/adr/ADR-DES.API.api-gateway-adoption.md`: аутентификация (OIDC-проверка токена) входит в функции гейтвея MVP; агентский трафик — MCP (токен).
- [x] Обновить `specs/qa/e2e-gui-testing.md` §11.3: «неавторизованная попытка отклоняется» — из целевого в обязательный сценарий MVP; добавить сценарий «истёкший/невалидный токен SSO».
- **Deliverable:** ADR SSO принят; API Gateway ADR и E2E GUI согласованы.
- **Зависит от:** Задача 1.3

### Задача 1.5 — Согласовать формулировки ПДн (2.1, A2)

- [x] Зафиксировать решение в `specs/vision.md` (§2.7 п.7, §3.2) и `specs/glossary.md` (при необходимости): «система не обрабатывает ПДн как бизнес-данные (корпус — код, спеки, документы); система не используется для услуг неограниченному кругу лиц; идентификационные данные SSO — минимальны, обрабатываются по корпоративной политике; фильтрация утечек блокирует ПДн, попадающие в промпты/ответы».
- [x] Заменить/уточнить «код, спеки, ПДн» в: `specs/business-rules/README.md` (кандидат L135), `specs/use-cases/README.md` (L100), `specs/domain/context-map.md`, `specs/nonfun-req/REQ-NFR-security.compliance.llm-contour.md`, `specs/qa/e2e-api-testing.md` §11.7, `specs/qa/smoke-testing.md` (риски), `specs/qa/gates.md` (`G2-SEC`) — единая формулировка с отсылкой к решению vision.
- **Deliverable:** нет противоречивых упоминаний ПДн; регуляторная позиция однозначна (не оператор ПДн; SSO — по корпоративной политике).
- **Зависит от:** —

### Задача 1.6 — Контур LLM: локальный + внешний по договору через шлюз (1.7, A3)

- [x] Обновить `specs/c4/context.md` (внешние системы, контур LLM): два допустимых варианта — локальные GPU (основной) и внешний провайдер по договору через корпоративный шлюз; оба — с фильтрацией утечек и аудитом.
- [x] Обновить `specs/nonfun-req/REQ-NFR-security.compliance.llm-contour.md`: критерий приёмки «доля обращений через корпоративный канал — 100%; вне контура — 0» — сохранить; добавить: «внешний провайдер — только по договору через корпоративный шлюз (фильтрация + аудит)»; «масштаб не ограничен только локальными GPU».
- [x] Обновить `specs/vision.md` §2.7 п.7/10: уточнить «внешние провайдеры — вне контура продукта» → «внешние провайдеры — только по договору через корпоративный шлюз (в рамках контура)».
- **Deliverable:** контур LLM допускает оба варианта исполнения; граница «0 вне контура» сохранена и непротиворечива.
- **Зависит от:** —

### Задача 1.7 — Ратифицировать контейнерную композицию и форму развёртывания (2.9)

- [x] `specs/adr/ADR-DES.INFRA.container-composition.md`: статус ПРЕДЛОЖЕНО → ПРИНЯТО; добавить решение о форме развёртывания — **Docker Compose** (однохостовой контур MVP; контейнеры C4 ≠ Docker-контейнеры — уточнить маппинг: какие C4-контейнеры являются сервисами compose); критерий пересмотра — горизонтальное масштабирование воркеров индексации (Фаза 2/3).
- [x] Обновить `specs/c4/container.md`: «форма развёртывания не зафиксирована» → «Docker Compose (MVP), один хост»; уточнить состав compose-сервисов (10 C4-контейнеров → сервисы, включая Neo4j CE и статик-сервер).
- [x] Обновить `specs/adr/README.md`: реестр — 9 принятых ADR.
- **Deliverable:** контейнерная модель ратифицирована; форма развёртывания зафиксирована (Docker Compose).
- **Зависит от:** —

---

## Фаза 2: Числовые NFR и QA-метрики (B1–B9)

### Задача 2.1 — NFR доступности и восстановления: RPO/RTO (1.3, 1.8)

- [x] Создать `specs/nonfun-req/REQ-NFR-api.availability.downtime-budget.md` (P1):
  - Бюджет простоя ≤ 15 мин/день суммарно на инстанс (planned + unplanned), «система не критичная».
  - RPO: 0 для исходных артефактов (источник правды — GitLab/S3, вне системы); индекс — производный, восстановление полной перестройкой (без RPO-гарантий на индекс).
  - RTO: восстановление ответов ≤ 15 мин на инцидент (в рамках дневного бюджета); откат к предыдущему опубликованному снапшоту без перестройки — уже обеспечен `REQ-NFR-data.maintainability.versioned-provenance`.
  - Верификация: restore-тест в CI (детерминированный: развёртывание из бэкапа dump → прогон эталонных кейсов K1/K4/K5/K17); периодичность — ежеквартально (предложение).
- [x] Обновить `specs/qa/test-plan.md` §8 (план тестирования NFR): добавить метрику «бюджет простоя» и restore-тест.
- **Deliverable:** ячейки 4.2/4.3 получили измеримые требования (RPO/RTO/budget).
- **Зависит от:** —

### Задача 2.2 — NFR защиты от перегрузки: стартовые лимиты агентов (1.4)

- [x] Обновить `specs/nonfun-req/REQ-NFR-api.performance.agent-traffic-isolation.md` (P1): зафиксировать стартовые консервативные значения (калибровка по телеметрии): бюджет агентского workflow — ≤ 50k токенов/запрос, ≤ 20 шагов извлечения, ≤ 10 вызовов инструментов; таймаут шага ≤ 30 с, workflow ≤ 10 мин; RPS агента ≤ 10 (стартовое); интерактивные запросы приоритетнее; превышение → 429/backoff.
- [x] Обновить `specs/use-cases/UC-mcp.compile.context.md` (A2): заменить placeholder «Q1.4 — placeholder-лимиты» на ссылку на NFR со значениями.
- [x] Обновить `specs/use-cases/UC-mcp.compile.context.md` (A3): промпт-инъекция — детекция, блокировка, `SecurityEventEmitted` — подтверждено как обязательное требование (алгоритм детекции — реализация: правила + LLM-классификатор).
- **Deliverable:** ячейка 4.2 закрыта измеримо (лимиты, таймауты, rate limit, инъекции).
- **Зависит от:** —

### Задача 2.3 — NFR наблюдаемости: золотые сигналы с мягкими порогами (1.6)

- [x] Расширить `specs/nonfun-req/REQ-NFR-api.observability.agent-audit.md` или создать `REQ-NFR-api.observability.golden-signals.md` (P1): публикуемые метрики — латентность ответа p50/p95, доля ошибок, насыщенность очередей задач, число LLM-вызовов, время публикации индекса; пороги — мягкие (warn, без жёсткого SLA): доля ошибок < 1%, насыщенность < 80%, время публикации индекса ≤ 15 мин (p95); on-call не требуется; алерты — через корпоративный мониторинг.
- [x] Обновить `specs/qa/test-plan.md` §8 и `qa/README.md` §5.1 (полнота QA по ЖЦ — строка «Сопровождение»).
- **Deliverable:** ячейки 3.3/5.3 получили измеримые метрики наблюдаемости.
- **Зависит от:** Задача 2.1 (согласование времени публикации)

### Задача 2.4 — NFR точности: benchmark-набор и инструмент оценки RAGAS (2.2, 3.5)

- [x] Обновить `specs/nonfun-req/REQ-NFR-api.compliance.rag-accuracy.md` (P0): benchmark-набор = эталонные кейсы K1/K4/K5/K17 + golden-наборы «запрос → эталонный ответ → источники» (определены в qa/); инструмент оценки — RAGAS (faithfulness, answer relevancy; LLM-as-judge); порог точности ≥ 70% на benchmark-наборе; валидация ссылок — реальные URL на артефакты GitLab/S3 (детерминированная проверка существования, UC-answers).
- [x] Обновить `specs/qa/gates.md`: добавить гейт `FG-RAG-EVAL` (RAGAS-прогон на golden-наборах в CI; faithfulness ≥ 70%; провал — блокер релиза); обновить `G2-FF` (фитнес-функции: RAG-качество).
- [x] Обновить `specs/qa/test-plan.md` §8 (метрика «Точность ответов RAG» → RAGAS + benchmark).
- **Deliverable:** точность ≥ 70% измерима детерминированно (инструмент + benchmark); гейт в CI.
- **Зависит от:** —

### Задача 2.5 — NFR актуальности индекса: целевые значения (3.7)

- [x] Обновить `specs/nonfun-req/REQ-NFR-data.performance.index-freshness.md` (P0): целевые значения — время от MR/события до публикации обновлённого индекса: медиана ≤ 5 мин, p90 ≤ 15 мин (инкрементальное обновление затронутого подграфа); доля ответов на актуальных версиях источников ≥ 95% (целевое, подтверждается замером Фазы 1.5); полная перестройка — фоновая, вне горячего пути.
- [x] Зафиксировать обоснование цифр в ADR/README или RESEARCH (практика: инкрементальные обновления GraphRAG/LightRAG — минуты для точечных изменений; полная перестройка 10k документов — часы, референс 281 мин/1 млн токенов CPU).
- [x] Обновить `specs/qa/test-plan.md` §8 (метрика «Инкрементальное обновление»).
- **Deliverable:** ячейки 1.2/3.3 закрыты измеримо по актуальности.
- **Зависит от:** —

### Задача 2.6 — Покрытие тестами: 100% требование-уровневое + пороги (3.8)

- [x] Обновить `specs/qa/unit-testing.md` §12: закрыть TBD — требование-уровневое покрытие FR/US/BR 100% (каждое требование ≥ 1 тест; гейт FG-TRACE); line/branch — критичные модули (контур LLM/фильтрация утечек, онтологический слой, извлечение, анализ влияния, MCP-компилятор, инкрементальное обновление) ≥ 80%; негативные сценарии обязательны.
- [x] Обновить `specs/qa/gates.md` `FG-COVERAGE` (порог задан), `FG-TEST`, `FG-TRACE`; `specs/qa/README.md` §5 (правило матрицы: P0/P1 → все FR/US/BR).
- [x] Обновить `specs/qa/test-plan.md` (критерии выхода).
- **Deliverable:** порог покрытия зафиксирован; гейты FG-* измеримы.
- **Зависит от:** —

---

## Фаза 3: US-каталог (1.2)

### Задача 3.1 — Расширить классификацию US доменами жизненного цикла (1.2, A4)

- [x] `specs/user-stories/README.md`: пересмотреть принцип «US — только функциональная польза» (vision §2.2): US покрывают также этапы ЖЦ. Добавить домены: `US-ops.*` (поддомены: `monitor` — мониторинг/алерты; `update` — обновление/откат; `restore` — восстановление из бэкапа) и `US-decommission.*` (поддомены: `stop` — остановка стека; `remove` — удаление томов/данных). Приоритеты: P2 (сопровождение), P2 (вывод).
- [x] Согласовать с `specs/qa/README.md` §5.1 (полнота QA по ЖЦ: колонки 3–4 — US-источники для E2E) и `gates.md` `G1-ID` (схема `US-<domain>.<subdomain>.<action>` — домены расширены).
- **Deliverable:** классификация US покрывает все этапы ЖЦ; принцип US README согласован с ответом 1.2.
- **Зависит от:** — (решение стейкхолдера по A4; дефолт плана)

### Задача 3.2 — Сгенерировать US-файлы (Gherkin) из UC (1.2)

- [x] Создать P0-US по шаблону US README (теги `@US-*`, `@UC-*`, `@P0`, негативные сценарии, `@accessibility` для UI):
  - `specs/user-stories/US-answers.grounding.cited-answer.md` (из `UC-answers.grounding.cited-answer`; + `@accessibility` сценарий для UI)
  - `specs/user-stories/US-knowledge.sync.gitlab-index.md` (из `UC-knowledge.sync.gitlab-index`)
  - `specs/user-stories/US-context.impact.analyze.md` (из `UC-context.impact.analyze`)
  - `specs/user-stories/US-mcp.compile.context.md` (из `UC-mcp.compile.context`; + сценарии A1–A4: токен/rate limit/инъекция/таймаут)
- [x] Создать P2-US для этапов ЖЦ (по классификации Задачи 3.1): `US-ops.monitor.health.md`, `US-ops.update.rollback.md`, `US-ops.restore.from-backup.md`, `US-decommission.stop.instance.md`, `US-decommission.remove.data.md` (CLI-канал, админ).
- [x] Обновить `specs/c4/context.md` (пользовательские путешествия: добавить 7–8 — сопровождение/вывод, если классификация расширена).
- **Deliverable:** каталог US-файлов в Gherkin по функциям F1–F4 и этапам ЖЦ; трассируемость US ↔ UC.
- **Зависит от:** Задача 3.1, Задача 1.3 (доступ: read-only/CLI)

---

## Фаза 4: Эргономика, окружение, автоматизация (1.5, 2.3, 2.10, 3.1–3.4, 3.6, 3.9)

### Задача 4.1 — Эргономика UI: WCAG 2.1 AA, локализация, браузеры (1.5, 2.10)

- [x] `specs/business-rules/BR-constraint.web-app-browser-chat.md`: зафиксировать WCAG 2.1 AA (уровень + базовые SC: keyboard, контраст, текстовые альтернативы); локализация — русский и английский, i18n-архитектура с возможностью добавления локалей (RTL не требуется); браузеры — evergreen 2026: Chrome/Edge/Firefox последние 2 мажорные версии, Safari последние 2; MCP-клиенты агентов — стандартные 2026 (JSON-RPC 2.0, MCP-спецификация).
- [x] `specs/qa/e2e-gui-testing.md` §11.4: «WCAG MVP» → «WCAG 2.1 AA»; a11y-сценарии `UI-A11Y-*` — из expected fail в обязательные (после фиксации требований); добавить локализационные проверки (ru/en переключение, отсутствие зашитых строк).
- [x] Обновить `specs/user-stories/README.md` §7 (Accessibility: «WCAG MVP» → WCAG 2.1 AA).
- **Deliverable:** ячейка 2.2 закрыта измеримо (уровень WCAG, языки, браузеры).
- **Зависит от:** —

### Задача 4.2 — Политика разработки: ИИ-генерация, spec-driven, стандарт (2.3)

- [x] `.ai-factory/RULES.md`: добавить раздел «Политика разработки» — ИИ-генерация с человеческим подтверждением (уже есть); spec-driven development (спецификации — источник правды, тесты из спецификаций); README-файлы — на русском; комментарии — человеко-читаемые (язык комментариев: русский для бизнес-логики, идентификаторы — английский; зафиксировать); максимальная автоматизация; стандартные инструменты (что возможно — максимально привычно).
- [x] `src/README.md`: синхронизировать с политикой (README на русском, spec-driven).
- **Deliverable:** политика разработки зафиксирована в RULES.md и src/README.
- **Зависит от:** —

### Задача 4.3 — Политика лицензий и риски open-source; реакция на утечку секретов (3.1, 3.2, 3.6)

- [x] Зафиксировать политику лицензий (в `specs/business-rules/BR-constraint.opensource-only.md` или в NFR/ADR): только open-source; допустимые лицензии — Apache-2.0, MIT, BSD-3-Clause, GPLv3 (Neo4j CE — принято в ADR); open-weight модели — лицензии проверяются SCA-гейтом (LLaMA/Qwen/Mistral — совместимость); риски (в RESEARCH.md): экспортные ограничения (EAR) на модели, GPL-копилефт, SCA-гейт (trivy) в CI.
- [x] Процедура реагирования на утечку секретов: детекция — `G2-SEC` + smoke `SM-B-09`; реакция — порядок: отозвать секрет → перевыпустить → просканировать историю репозитория → ротация зависимых; срок реакции — в рамках инцидентного процесса команды (без жёсткого SLA, «не критично»). Зафиксировать в `specs/qa/smoke-testing.md` (SM-B-09 расширить) или BR.
- [x] Фильтрация утечек (3.6): типы детектируемых данных — токены, ключи, ПДн; детектор — правила (regex/энтропия) + классификатор (реализация); зафиксировать в `REQ-NFR-security.compliance.llm-contour.md` (перечень типов).
- **Deliverable:** политика лицензий, риски и процедура реагирования зафиксированы; типы фильтрации утечек определены.
- **Зависит от:** Задача 1.5 (формулировки ПДн)

### Задача 4.4 — CI/CD: GitLab CI, автоматизация, MCP-серверы (3.3, 3.9, 2.9)

- [x] `specs/qa/README.md` §8 (целевое состояние CI/CD): зафиксировать платформу — GitLab CI; цепочка: golangci-lint → `go test -race` → сборка → smoke (деплой Docker Compose тестового контура) → интеграционные → e2e → NFR (RAGAS) → публикация; деплой — Docker Compose (см. Задача 1.7).
- [x] `.ai-factory.json`: включить MCP-серверы `playwright` (E2E GUI-автоматизация; ответ 3.9 «если можно автоматизировать — автоматизировать»); обосновать в RESEARCH.md (расхождение конфигурации с QA-стратегией устранено).
- **Deliverable:** CI/CD-платформа и автоматизация зафиксированы; конфигурация агента согласована с QA.
- **Зависит от:** Задача 1.7

---

## Фаза 5: Закрытие open-questions и верификация

### Задача 5.1 — Обновить open-questions.md (статусы ответов)

- [x] `specs/open-questions.md`: отметить каждый вопрос статусом ответа: закрыт с решением (Q1.3–1.10, 2.1–2.10, 3.1–3.9 — по ответам); остаточные TBD — «после замера исходного уровня (Фаза 1.5)» (Q2.2-метрики, 3.7-подтверждение); обновить сводную матрицу (ожидаемые статусы после фиксации: ячейки 1.4/2.4/3.4/4.4/5.4 — закрыты решениями декомиссии; 2.2/4.2 — закрыты NFR; 5.2 — закрыта окружением; остаются TBD-подтверждения после замера); обновить вердикт.
- **Deliverable:** реестр открытых вопросов отражает ответы; остаются только подтверждаемые замером TBD.
- **Зависит от:** все задачи Фаз 1–4

### Задача 5.2 — Прогнать гейты согласованности и матрицу качества

- [x] Прогнать гейты по изменённым артефактам: `G1-LINK` (ссылки), `G1-ID` (схемы ID, включая новые BR/ADR/US), `G1-TRACE` (трассируемость vision→UC→US→тест), `G1-TBD` (реестр TBD в RESEARCH.md), `G1-METRIC` (измеримость новых NFR), `G1-GHERKIN` (синтаксис US-файлов).
- [x] Кросс-ревью согласованности (аналог `G2-CONSIST`): FR/NFR/BR/UC/US/ADR не конфликтуют (особенно: BR доступа vs API Gateway ADR vs E2E GUI; ПДн-формулировки; контур LLM).
- [x] Прогнать `quality-matrix` (скилл, `G2-QMATRIX`) по обновлённым артефактам: подтвердить закрытие ячеек колонки 4 и ячеек 2.2/4.2/5.2; обновить `specs/qa/README.md` §5 (матрица качества, если затронута).
- [x] Прогнать `aif-security-checklist` (`G2-SEC`): секреты не попали в артефакты; регуляторная позиция согласована; лицензии отражены.
- **Deliverable:** гейты согласованности пройдены; матрица программирования качества обновлена (ожидаемо: 🔴 5 → 0 в колонке 4, 🟠 4 → 0, остаются 🟡 с TBD после замера).
- **Зависит от:** Задача 5.1

---

## Commit Plan

| № | Коммит | Задачи |
|---|---|---|
| 1 | `docs(adr): align language stack, ratify container composition and docker-compose` | 1.1, 1.2, 1.7 |
| 2 | `docs(specs): add SSO access model and reconcile PII and LLM-contour statements` | 1.3, 1.4, 1.5, 1.6 |
| 3 | `docs(nfr): set availability, load, observability and freshness targets` | 2.1, 2.2, 2.3, 2.5 |
| 4 | `docs(qa): add RAG quality gate (RAGAS) and test coverage policy` | 2.4, 2.6 |
| 5 | `docs(user-stories): extend US domains to lifecycle and generate US files` | 3.1, 3.2 |
| 6 | `docs(specs): finalize UI ergonomics, dev policy, licenses and CI automation` | 4.1, 4.2, 4.3, 4.4 |
| 7 | `docs(open-questions): close answered questions and run consistency gates` | 5.1, 5.2 |

---

## Критерии завершения

- 6 противоречий (A1–A6) устранены: нет конфликтующих формулировок в артефактах.
- 9 частичных ответов (B1–B9) зафиксированы измеримо в NFR/BR/QA (значения по дефолтам плана).
- 13 полных ответов (C) внесены в артефакты (ADR, BR, NFR, QA, US, RULES).
- Гейты `G1-*`, `G2-CONSIST`, `G2-QMATRIX`, `G2-SEC` пройдены; `open-questions.md` отражает статусы.
- Матрица программирования качества: колонка 4 закрыта, 🟠 = 0; остаются 🟡 с TBD «после замера исходного уровня (Фаза 1.5)».
