# BR-constraint.sso-readonly-access

| Поле | Значение |
|------|----------|
| **ID** | `BR-constraint.sso-readonly-access` |
| **Тип правила** | `constraint` (ограничение) |
| **Приоритет** | High |
| **Статус** | `approved` |
| **Источник** | Решение владельца продукта (2026-08-30); замена `BR-constraint.no-user-auth-in-mvp` |
| **Связанные требования** | REQ-NFR-security.compliance.llm-contour (фильтрация утечек, события безопасности контура действуют); ADR-DES.SECURITY.sso-keycloak; ADR-DES.API.api-gateway-adoption; vision.md §2.7 п.7 (LLM только в контролируемом контуре) |
| **Связанные BR** | BR-constraint.no-user-auth-in-mvp (ЗАМЕНЕНО) |

## Формулировка правила

Доступ людей к системе — через **корпоративный SSO (Keycloak, OIDC)**; по умолчанию добавлен **один пользователь `User`**; **ролей нет** (RBAC не вводится); **доступ к знаниям — только на чтение** (read-only: никаких write-операций с данными знаний через API/UI/MCP); **данные приложения (например, история сессий веб-интерфейса, `BR-constraint.ui-session-history`) — записываемые** (решение владельца продукта, 2026-08-30). ИИ-агенты автоматизации разработки подключаются **по токену** (агентский токен, выдаётся через корпоративный процесс) через MCP-канал (F4). Аудит действий пользователя — на уровне SSO (кто обращался); аудит обращений к LLM на уровне контура безопасности (`SecurityEventEmitted`) действует в полном объёме.

**Граница:** read-only-режим не отменяет сквозное требование `REQ-NFR-security.compliance.llm-contour` — фильтрация утечек, аудит и события безопасности контура остаются в силе; развёртывание — только в доверенном периметре.

## Обоснование

ИИ-агенты могут обращаться и через человеческий шлюз (API Gateway), поэтому аутентификация людей необходима уже в MVP (решение владельца продукта, 2026-08-30). RBAC не нужен: система — внутренняя, единый пользователь, доступ к знаниям — read-only; роли могут быть добавлены в будущих версиях при появлении административных функций или расширении прав. Прежнее правило «авторизация отсутствует в MVP» (`BR-constraint.no-user-auth-in-mvp`) заменено: без аутентификации нельзя отличить агента от человека на общем шлюзе.

## Критерии проверки

- В контракте API отсутствуют write-эндпоинты для данных знаний (создание/изменение/удаление источников корпуса); write-эндпоинты данных приложения (например, история сессий UI, `BR-constraint.ui-session-history`) допустимы (детерминированная сверка по OpenAPI-схеме)
- Запрос человека без валидного OIDC-токена SSO → отказ с кодом ошибки; неавторизованная попытка фиксируется
- Агентский запрос через MCP без валидного токена → отказ с кодом ошибки (UC-mcp A1)
- В системе ровно один пользователь по умолчанию (`User`); ролей/прав нет (нет RBAC-модели)
- События фильтрации утечек и аудита контура LLM (`SecurityEventEmitted`) фиксируются по-прежнему
- Действия пользователя (кто обратился, когда) воспроизводятся по журналу SSO/API Gateway

## Открытые вопросы

— (нет)
