# REQ-NFR-api.observability.golden-signals — Наблюдаемость: золотые сигналы с мягкими порогами

| Поле | Значение |
|------|----------|
| **ID** | `REQ-NFR-api.observability.golden-signals` |
| **Приоритет** | P1 |
| **Статус** | `proposed` |
| **Источник** | Решение владельца продукта (2026-08-30): «В целом, не критично. Добавь стандартные не очень жёсткие метрики»; vision.md §3.1 (профиль «Администратор системы», «IT-безопасность» — панели аудита); §2.4 (`SecurityEventEmitted`) |
| **Связанные функции** | F1–F4 (сквозное) |
| **Связанные требования** | REQ-NFR-api.observability.agent-audit (аудит агентских шагов); REQ-NFR-api.performance.query-responsiveness; REQ-NFR-data.performance.index-freshness; ADR-DES.INFRA.container-composition (контейнер «Мониторинг и аудит») |

## Описание

Наблюдаемость по стандартным золотым сигналам SRE с **мягкими порогами** (уровень warn, без жёсткого SLA и без on-call-дежурств — система «не критичная»). Метрики публикуются в контейнер «Мониторинг и аудит» (C4) и потребляются корпоративным мониторингом/панелями ИБ.

## Критерии приёмки

- Публикуемые метрики (на инстанс): латентность ответа p50/p95; доля ошибок; насыщенность очередей задач; число LLM-вызовов; время публикации индекса (MR → публикация); события фильтрации утечек (`SecurityEventEmitted`)
- Мягкие пороги (warn): доля ошибок < 1%; насыщенность очередей < 80%; время публикации индекса ≤ 15 мин (p95); латентность p95 — целевое значение после замера исходного уровня (vision §1.4 G1)
- Алерты — через корпоративный мониторинг (интеграция); on-call/дежурства не предусмотрены; инцидентный процесс — командой разработки
- Метрики доступны в UI администратора (статусы индексации, метрики — BR-constraint.web-app-browser-chat)
